# Policy mapping and open interpretation questions

For BA Labs. Maps each line of the "Morpho Vaults v2 Eligibility Criteria" document (snapshot 2026-09-01, last changed 2026-08-31) to a check, and lists the interpretation calls the tool makes. Each call is a config value or a documented rule; nothing here is hidden in code.

| Policy line | Check | Interpretation in the tool | Confidence |
|---|---|---|---|
| Owner = Prime SubProxy | C4 | On Ethereum the SubProxy; on Base and Robinhood the Prime's governance executor, since the registries and the Atlas label those as the Prime's governance arm there. An n-of-n Safe that includes the governance address is WARN (veto, not control). | High for Ethereum; the executor reading for other chains should be confirmed |
| Curator = 2/2 msig between OEA and external msig | C5 | Threshold 2 of 2 owners, one owner labeled as an external curator by Morpho's curator registry, the other the OEA. The OEA half counts as attributed when a public source labels it directly or names the pair (the Atlas: "Curator: Soter Labs and Sentora, … 2 of 2"); the keys inside are never expected to be labeled. A co-signer nothing names is WARN (a disclosure gap, since 2026-09-14 the case of the two new Grove vaults, whose Atlas article does not yet describe their curator). Whether the OEA half is a Safe or a single key does not change the verdict. A Prime- or OEA-only Safe (for example the Spark 3/5) is WARN, not FAIL. | Open question 1: does the 2/2 rule apply to Prime-self-curated vaults? Open question 2 (single key on the OEA side) is recorded, not graded. |
| Allocator = Prime Agent / external curator EOA / multisig | C9 | Reported, never graded. The Morpho Public Allocator is acceptable per the criteria comment of 2026-09-10 (BA Labs recommends pairing emergency safeguards with a transaction setting its caps to 0); once its address is cited in the policy it is labeled as such. | High |
| Sentinel = OEA with a separate set of signers; the only mandatory sentinel | C6 | At least one sentinel that is a Safe, attributed to the OEA, whose leaf signers do not intersect the curator Safe's leaf signers. | Open question 2: is a single-EOA OEA co-signer on the curator Safe acceptable, and is a re-roled v1.1 vault (SubProxy owner, OEA Guardian with separate signers) acceptable instead of Vault V2? |
| "and/or monitoring solution, and/or prime-owned msig"; "Additional Sentinel addresses are optional and recommended" | C6 | The curator address itself may not hold a seat (FAIL, adopted 2026-09-08). Prime-owned extra seats are allowed. Any other party in an extra seat, the external curator, a signer of the curator Safe, a Safe sharing signers with the curator Safe, or an unattributed address, is WARN until BA answers. | Open question 4: may a party other than the OEA and the Prime hold an additional sentinel seat? The document lists OEA, monitoring solution and Prime-owned multisig; it does not say whether the list is exhaustive. |
| v1.1 vaults | C8, C6, C7 | A v1.1 vault is not a gap by itself; its Guardian is judged as the sentinel seat; per-function timelocks do not apply. | Medium |
| Accepted collateral and max LLTV | C3 | Address allow-list per chain; ETH means WETH, stETH means wstETH or stETH. A market counts when it has an allocation of at least one whole unit or a non-zero cap. Not accepted with an allocation is FAIL; cap-only is WARN. | High; cbETH and spUSDG proposed for approval on 2026-09-02, not applied |
| IRM column: Morpho Adaptive Curve IRM (v1 markets only) | C3 | Every market's IRM must equal the Adaptive Curve IRM address for its chain, read from Morpho Blue's market parameters and from the API. Reference addresses come from the Spark registry (MORPHO_DEFAULT_IRM) on Ethereum and Base; on Robinhood no registry publishes it, so the API's market data is the reference until one does. Morpho Blue's isIrmEnabled is shown as supporting evidence. Markets V2 have no IRM and are not graded on it. | High on Ethereum and Base; medium on Robinhood (reference address not registry-published) |
| Loan assets | C2 | Address allow-list per chain: USDC, USDT, PYUSD, RLUSD, USDG, plus USDS on Ethereum and Base since 2026-09-10 by Sky GovOps decision: the document names neither its inclusion nor its exclusion, no public decision excludes it, and it is the loan asset of the Spark USDS Morpho vault, of the sky.money vaults and of the Atlas Integration Boost for USDS in Morpho. DAI is not added. | Open question 5: confirm USDS as an accepted loan asset, and say whether DAI is. |
| Accepted chains | C1 | Ethereum, Base, Robinhood. Any other chain is FAIL with the policy's own wording (risk review and separate request). | High |
| Timelock minimums per function, vault and adapter | C7 | Selectors derived from the Vault V2 and adapter function signatures; "7d/Abdicated" accepted as abdicated for the receive shares, send shares and receive assets gates and the adapter registry. Abdication counts only on the rows marked '/Abdicated' and fails every other row regardless of the timelock value (adopted 2026-09-16 after BA Labs asked for abdicated send-assets gates to be flagged). The send assets gate is listed as "7d" only and is read literally: abdication does not satisfy it. Below minimum is FAIL. | Open question 3: severity of a timelock below minimum (FAIL assumed). If BA intends abdication to be acceptable for the send assets gate too, one config value changes. Since 2026-09-10 (allocator) and 2026-09-14 (force-deallocate penalty) the criteria allow no delay on those two functions; the tool grades a remaining delay FAIL (Sky GovOps decision of 2026-09-15); BA Labs' 'no action required now' is recorded in the policy notes but does not soften the grade. |
| Oracle criteria, interim single-source Chainlink | C10 | Reported only. | Pending BA |
| Fees TBD | C11 | Reported only. | Pending BA |
| Deadline and 100% CRR on non-compliant allocations | C12 | Exposure and Liquidity Layer onboarding shown as context so a reader sees what a verdict means in money; not graded. | n/a |

## What public sources can and cannot prove

Structure is provable: Safe version, threshold, owner set, signer disjointness, timelocks, caps, LLTVs. Prime-side and curator-side identity is provable from the registries, the Atlas and Morpho's curator registry. OEA identity is not: on 2026-09-04 no public source labels the OEA sentinel Safes or the OEA co-signers. The tool grades those seats WARN with the exact reason, and an unlabeled Safe that shares a signer with a publicly labeled Safe is attributed "by signer overlap", which can lower a verdict but never raise it to PASS.

Recommendation to turn the OEA seats green from a public source: list the OEA Safe addresses per chain in the Atlas Ozone OEA article (A.6.1.2.2) or in a Soter-published address registry in the same format as the Prime registries. The label sync would pick either up automatically.
